export * from './dto.auth';
//exporting in this format by creating the index file is called barrens export.
//here all the files get exported through the index file and while importing we can specify the required import in the {} brackets.