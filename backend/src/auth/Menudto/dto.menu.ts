import { IsEmail, IsJSON, IsNotEmpty, IsOptional, IsString } from "class-validator";


export class MenuDto{
    // @IsEmail()
    // @IsNotEmpty()
    // myemail: string;

    // @IsNotEmpty()
    // @IsJSON()
    // menu: JSON;
}